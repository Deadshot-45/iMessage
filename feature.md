# Features

## Chat
1. When user send message only one gray tick should apply, not two 
2. when message send to the user then two gray tick apply.
3. when user read the message in chat then two blue tick apply.

## Media 
1. reciever user can only see the image after download or preview is visible 
2. send user can see the image preview immediately 
3. when user preview the image then image open in fullscreen mode 
4. Same for video, audio and gif, reciever user cannot preview video and gif until download in case of video for audio it can play in background.
5. in chat screen when media is sent it should show thumbnail preview and progress bar for uploading if not uploaded yet or if not downloaded yet.
6. when media is deleted it should show deleted message.
7. in chat screen show icon for delete in the corner of media, and show full screen icon when media is downloaded.
